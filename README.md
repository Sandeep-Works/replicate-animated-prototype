
  # Replicate animated prototype

  This is a code bundle for Replicate animated prototype. The original project is available at https://www.figma.com/design/qSqR8UlHaqW6ad5z68MaPR/Replicate-animated-prototype.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  